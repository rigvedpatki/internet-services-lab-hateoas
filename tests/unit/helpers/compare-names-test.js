import { compareNames } from 'internet-services-lab-hateoas/helpers/compare-names';
import { module, test } from 'qunit';

module('Unit | Helper | compare names');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compareNames([42]);
  assert.ok(result);
});
